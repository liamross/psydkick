{
  "name": "psydkick-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "start": "rm -rf build && tsc && node build/index.js",
    "build": "tsc"
  },
  "dependencies": {
    "apollo-server": "^2.3.1",
    "graphql": "^14.0.2"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "tslint": "^5.12.0"
  }
}
