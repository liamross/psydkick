{
  "name": "psydkick-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "start": "nodemon build/index.js",
    "watch": "rm -rf build && tsc --watch",
    "build": "rm -rf build && tsc",
    "predefs": "apollo service:download --endpoint=http://localhost:4000/graphql",
    "defs": "apollo codegen:generate src/types/api.d.ts --target typescript --outputFlat"
  },
  "dependencies": {
    "apollo-server": "^2.3.1",
    "graphql": "^14.0.2",
    "reflect-metadata": "^0.1.13",
    "sqlite3": "^4.0.6",
    "typeorm": "^0.2.11"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "apollo": "^2.1.9",
    "jest": "^23.6.0",
    "nodemon": "^1.18.9",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  }
}
